# 로컬 BFF → 백엔드 (pnpm dev). 값 있으면 Production이 아닐 때 upstream으로 사용.
MINI_LOCAL_API_URL=http://127.0.0.1:37651

# Vercel Production / Preview — 대시보드 Environment Variables 에만 등록 (Git·로컬 .env 금지)
# API_URL=http://43.201.95.108:8080

# 선택. next.config 가 빌드 ID 로 읽음 (gitignore). CI·로컬 빌드 스탬프용.
# NEXT_PUBLIC_BUILD_ID=local-dev
